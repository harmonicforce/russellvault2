node_modules/
*.log
.DS_Store
dist/

# Generated data exports / review files (one-off, not app source)
*_for_review.csv
*_remaining.csv
*_by_seller.csv
review_*.csv
/server/data/

# Env files — never commit real values; .env.example placeholders are tracked
.env
.env.*
*.env
!.env.example

# SQLite databases and WAL/SHM state — never commit live or backup databases
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite3

# Backups and G0A preflight evidence (filled manifests, retrieved backups)
backups/
preflight-evidence/
*.backup

