# Root environment placeholders. Runtime configuration for this monorepo lives in
# the server (see server/.env.example) and client (see client/.env.example).
# No secrets belong in any committed .env.example file.
